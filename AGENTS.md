# AGENTS.md

Project type:
> depending on the git branch

`master` branch:

This is a GitHub profile repository for `monyx-nuked`
<!-- change the username if you forked/copied this project-->
The most important file is `README.md`, it is used to tell about user in github profile page: github.com/`username`

This project branch has no CI/build systems, no tests as of currently
<!-- FIXME: implement tests CI using github actions -->

## Your job:
To help me maintain and make the code cleaner of this README profile.

This is **personal portofilio and anyone can copy this project ( as you see I have added LICENSE.md ), mainly not a code project but the other branches are because

The reason it is like that is to make this repository unified and so it can be archived or parts of it can not be lost

---

### Structure
- `README.md` - The profile page ( a.k.a the main page )
- `assets` - Resources for page, if exists

### Content guidelines
- Tone: friendly, silly, casual
- Voice: first person ( solo `developer` )
- Keep it simple, concise: the profiles are skimmed, not like a book to read
- Avoid walls of text: use hierarchy ( headers, badges, tablets )

### Markdown functionality
- GitHub profiles have pros:
    - can render `markdown` + `some HTML`
    - emojis are supported natively
    <!-- FIXME: add what stats providers to use -->

### do NOTS
- No additional pages, only `README` is rendered
- No large images, `assets` folder exist or hosted images
- Never add secrets, tokens, keys


### When making changes
- Preview mentally: will this look good on both dark `AND` light GitHub themes?
- Keep the README skimmable — a visitor should "get it" in under 10 seconds
- If adding a new section, place it logically (intro -> skills -> projects -> contact)