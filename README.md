### My hackintosh configuration ( OpenCore/EFI )

- Do note that:
    - It doesn't work to install macOS ( create new one instead )
    - It has different PlatformInfo settings ( pray If i didn't or report please :3 )
    - ApECID is 0, remember to change it to yours, for safety
    - This config uses custom OC Build (NO_ACPI)
    - Vault files cleaned up